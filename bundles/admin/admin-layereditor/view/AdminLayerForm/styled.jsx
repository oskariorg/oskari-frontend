import { Button } from 'oskari-ui';
import styled from 'styled-components';

export { StyledFormField, StyledAlert } from '../styled';

export const StyledRoot = styled('div')`
    padding: 20px;
    max-width: 700px;
`;
export const StyledButton = styled(Button)`
    margin-right: 5px;
`;
