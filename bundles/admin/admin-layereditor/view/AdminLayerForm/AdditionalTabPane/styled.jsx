import { SelectOutlined } from '@ant-design/icons';
import styled from 'styled-components';

const linkColor = '#0091ff';

export const MetadataButton = styled(SelectOutlined)`
    margin-left: 10px;
    color: ${linkColor};
`;
